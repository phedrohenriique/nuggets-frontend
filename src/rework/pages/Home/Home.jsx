import React from 'react'
import { Box } from '@chakra-ui/react'
import { pages } from '../../config/styles'

export const Home = (props) => {
    return (
        <Box
            style={pages}
        >

        </Box>
    )
}